Member photos go here.

Reference them from `src/content/members.ts` as `/images/members/<filename>`,
for example:

    photo: "/images/members/jkim.jpg",

Square images around 600×600 px look best. A member without a `photo` falls
back to their initials, so you can add people before you have pictures.
